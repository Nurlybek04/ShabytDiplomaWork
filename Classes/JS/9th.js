// Function to handle subject navigation
function openSubject(subject) {
    alert(`Сіз ${subject.charAt(0).toUpperCase() + subject.slice(1)} пәнін тандадыныз!`);
    // You can replace this with actual navigation logic
    // Example: window.location.href = `${subject}.html`;
  }